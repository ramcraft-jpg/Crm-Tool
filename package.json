{
    "name": "crm-root",
    "private": true,
    "scripts": {
        "frontend": "cd nexus_crm_frontend/frontend && npm run dev",
        "backend": "cd nexus_crm_backend && npm start",
        "dev": "concurrently \"npm run backend\" \"npm run frontend\""
    },
    "devDependencies": {
        "concurrently": "^8.2.2"
    },
    "dependencies": {
        "react-icons": "^5.6.0"
    }
}
